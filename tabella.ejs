<!DOCTYPE html>
<html>
<head>
  <title>Erogazioni a Sud Chiama Nord (SCN) e Sicilia Vera (SV)</title>
  <style>
    table { border-collapse: collapse; width: 100%; margin-top: 16px;}
    th, td { border: 1px solid #333; padding: 4px;}
    th { background: #eee; cursor: pointer;}
    body { font-family: sans-serif; margin: 24px;}
    .filter-bar { margin-bottom: 16px;}
    .filter-row { display: flex; align-items: center; gap: 18px; margin-bottom: 6px;}
    label { font-size: 14px;}
    .count-bar { font-size: 18px; margin-bottom: 6px; color: #23447e; font-weight: bold;}
    .sum-bar { font-size: 18px; margin-bottom: 16px; color: #044f09; font-weight: bold;}
    .arrow { font-size: 13px;}
    .btn-filtra {
      background-color: #0056b3;
      color: white;
      border: none;
      padding: 6px 16px;
      font-size: 16px;
      border-radius: 4px;
      cursor: pointer;
      font-weight: bold;
    }
    .btn-azzera {
      background-color: #c10020;
      color: white;
      border: none;
      padding: 6px 16px;
      font-size: 16px;
      border-radius: 4px;
      cursor: pointer;
      font-weight: bold;
    }
    .button-row { display: flex; gap: 18px; margin-top: 12px;}
    .filtro-partito {
      background: #ffcf8c;
      border-radius: 7px;
      padding: 4px 12px 4px 8px;
      margin-right: 18px;
      display: flex;
      align-items: center;
      gap: 12px;
      font-weight: bold;
    }
    .filtro-anno {
      background: #c3fdb8;
      border-radius: 7px;
      padding: 4px 16px 4px 8px;
      display: flex;
      gap: 14px;
      align-items: center;
      font-weight: bold;
    }
    .filtro-soggetto {
      background: #c8eafe;
      border-radius: 7px;
      padding: 4px 20px 4px 8px;
      display: flex;
      align-items: center;
      gap: 16px;
      font-weight: bold;
    }
    .filtro-gruppo-giallo1 {
      background: #fff9cc;
      border-radius: 7px;
      padding: 10px 20px;
      font-weight: bold;
      margin-right: 24px;
      min-width: 200px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .filtro-gruppo-giallo2 {
      background: #fdf6c0;
      border-radius: 7px;
      padding: 10px 20px;
      font-weight: bold;
      margin-right: 24px;
      min-width: 200px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .filtro-gruppo-giallo3 {
      background: #faedc7;
      border-radius: 7px;
      padding: 10px 20px;
      font-weight: bold;
      min-width: 200px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .filtro-gruppo-arancio1 {
      background: #ffe6c0;
      border-radius: 7px;
      padding: 10px 20px;
      font-weight: bold;
      margin-right: 24px;
      min-width: 200px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .filtro-gruppo-arancio2 {
      background: #ffedcc;
      border-radius: 7px;
      padding: 10px 20px;
      font-weight: bold;
      min-width: 200px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .filtro-gruppo-arancio-s1 {
      background: #ffd39b;
      border-radius: 7px;
      padding: 10px 20px;
      font-weight: bold;
      margin-right: 24px;
      min-width: 200px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .filtro-gruppo-arancio-s2 {
      background: #ffcd71;
      border-radius: 7px;
      padding: 10px 20px;
      font-weight: bold;
      margin-right: 24px;
      min-width: 200px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .filtro-gruppo-arancio-s3 {
      background: #ffb347;
      border-radius: 7px;
      padding: 10px 20px;
      font-weight: bold;
      min-width: 200px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .td-importo { text-align: right; }
  </style>
  <script>
    function sortBy(field) {
      const params = new URLSearchParams(window.location.search);
      const direction = params.get('sortField') === field && params.get('sortDir') === 'asc' ? 'desc' : 'asc';
      params.set('sortField', field);
      params.set('sortDir', direction);
      window.location.search = params.toString();
    }
    function azzeraForm() {
      window.location.href = window.location.pathname;
    }
  </script>
</head>
<body>
  <!-- INTESTAZIONE SU TRE RIGHE -->
  <div style="font-size:23px; font-weight:bold; color:#23447e; margin-bottom:2px;">
    Erogazioni a Sud Chiama Nord e Sicilia Vera
  </div>
  <div style="font-size:18px; font-weight:bold; color:#333; margin-bottom:2px;">
    Elenchi tratti dal sito del Parlamento Italiano
  </div>
  <div style="font-size:17px; color:#23447e; margin-bottom:12px;">
    <a href="https://parlamento18.camera.it/199" target="_blank" style="color:#23447e; text-decoration:underline;">
      https://parlamento18.camera.it/199
    </a>
  </div>
  <hr style="margin-bottom:20px;">
  <div class="filter-bar">
    <form method="get">
      <!-- [filtri a colori qui, come nella tua versione funzionante] -->
      ...
      <div class="button-row">
        <button type="submit" class="btn-filtra">FILTRA</button>
        <button type="button" class="btn-azzera" onclick="azzeraForm()">AZZERA</button>
      </div>
      <input type="hidden" name="sortField" value="<%= sortField || "" %>">
      <input type="hidden" name="sortDir" value="<%= sortDir || "asc" %>">
    </form>
  </div>
  <div class="count-bar">
    Record filtrati: <%= dati.length %>
  </div>
  <% if (typeof sommaImporti !== 'undefined' && sommaImporti !== null) { %>
    <div class="sum-bar">
      Somma Importi: <%= sommaImporti.toLocaleString('it-IT', { minimumFractionDigits:2, maximumFractionDigits:2 }) %>
    </div>
  <% } %>
  <table>
    <tr>
      <% if (dati.length > 0) {
        Object.keys(dati[0]).forEach(function(chiave){ %>
        <th onclick="sortBy('<%= chiave %>')" title="Ordina per <%= chiave %>">
          <%= chiave %>
          <% if (sortField === chiave) { %>
            <span class="arrow"><%= sortDir === 'asc' ? '▲' : '▼' %></span>
          <% } %>
        </th>
      <% }); } %>
    </tr>
    <% dati.forEach(function(riga){ %>
      <tr>
        <% Object.keys(riga).forEach(function(chiave, idx){ %>
          <td class="<%= chiave === 'Importo' ? 'td-importo' : '' %>">
            <% if (riga[chiave] === "TRUE") { %>SI<% } else if (riga[chiave] === "FALSE") { %>NO<% } else { %><%= riga[chiave] %><% } %>
          </td>
        <% }); %>
      </tr>
    <% }); %>
  </table>
</body>
</html>
